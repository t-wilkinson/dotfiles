# If a config.py file exists, this file is ignored unless it's explicitly loaded
# via config.load_autoconfig(). For more information, see:
# https://github.com/qutebrowser/qutebrowser/blob/master/doc/help/configuring.asciidoc#loading-autoconfigyml
# DO NOT edit this file by hand, qutebrowser will overwrite it.
# Instead, create a config.py - see :help for details.

config_version: 2
settings:
  bindings.commands:
    global:
      normal:
        ',m': spawn umpv.py {url}
        <Ctrl+g>: open -t https://greensock.com/docs/
        <Ctrl+h>: open -t https://hoogle.haskell.org/
        <Ctrl+m>: open -t https://developer.mozilla.org/en-US/
        <Ctrl+w>: open -t https://en.wikipedia.org/wiki/Wikipedia:Contents
        <Ctrl+y>: open -t https://youtube.com
  content.media.audio_capture:
    https://us05web.zoom.us: true
  content.notifications.enabled:
    https://app.slack.com: true
    https://mail.google.com: true
